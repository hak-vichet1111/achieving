# Root Compose environment for Achieving project

# Backend service
PORT=8080
ALLOW_ORIGIN=http://localhost:5173
DB_HOST=db
DB_PORT=3306
DB_USER=achieving
DB_PASS=achieving
DB_NAME=achieving_db
JWT_SECRET=dev-secret

# MySQL container initialization
MYSQL_DATABASE=achieving_db
MYSQL_USER=achieving
MYSQL_PASSWORD=achieving
MYSQL_ROOT_PASSWORD=rootpass

# Backend migration behavior
# Set to "true" to skip legacy ALTER TABLE alignment steps.
# AutoMigrate will still run to create/update core tables and constraints.
DISABLE_LEGACY_MIGRATIONS=true

# Frontend (Vite) build-time vars
# Leave empty in dev to use Vite proxy; set in prod if needed
VITE_API_BASE=http://localhost:8080

# Notes:
# 1) Copy this file to .env (at project root) and edit as needed.
# 2) docker compose will automatically read .env for ${...} expansions.
# 3) Services also use backend/.env and frontend/.env; values from .env
#    override defaults for Compose-managed envs.